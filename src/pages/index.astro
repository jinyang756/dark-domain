/* *****************************************************************
 * 文件一：src/layouts/ArticleLayout.astro (文章布局 - 最终暗色版)
 * 我们将用这个统一的、为暗色主题设计的“装修风格”，来解决黑白不匹配的问题。
 * *****************************************************************/
---
// 文章布局 V3.0 - 暗色沉浸式阅读
import '../styles/global.css';
const { frontmatter } = Astro.props;
---
<html lang="zh-CN">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>{frontmatter.title} - 暗域</title>
</head>
<body class="reading-view">
    <main class="reading-container">
        <header class="reading-header">
            <h1>{frontmatter.title}</h1>
        </header>
        <article class="prose">
            <slot />
        </article>
    </main>
    <a href="/" class="return-link">&larr; 返回世界之门</a>
</body>
</html>
<style>
/* 为阅读页面定制的body样式 */
:global(body.reading-view) {
    background-color: #111115; /* 深邃的阅读背景 */
    color: var(--color-text);
    padding: 4rem 2rem;
    overflow-y: auto; /* 允许长文章滚动 */
}

/* 阅读内容容器 */
.reading-container {
    max-width: 800px;
    margin: 0 auto;
    opacity: 0;
    animation: fadeIn 1s ease forwards;
}

/* 文章标题 */
.reading-header h1 {
    font-size: 3rem;
    color: var(--color-heading);
    border-bottom: 2px solid var(--color-accent-purple);
    padding-bottom: 1rem;
    margin-bottom: 2rem;
    text-align: center;
}

/* 文章正文样式 */
.prose {
    font-size: 1.1rem;
    line-height: 1.8;
}
.prose :global(h2), .prose :global(h3) {
    color: #e0e0e0;
    border-bottom: 1px solid #444;
    padding-bottom: 0.5rem;
    margin-top: 2.5rem;
}
.prose :global(a) {
    color: var(--color-accent-blue);
}
.prose :global(blockquote) {
    border-left: 3px solid var(--color-accent-purple);
    padding-left: 1.5rem;
    margin-left: 0;
    color: #aaa;
    font-style: italic;
}

/* 返回链接 */
.return-link {
    position: fixed;
    top: 30px;
    left: 30px;
    color: #888;
    background: rgba(0,0,0,0.3);
    padding: 10px 15px;
    border-radius: 5px;
    text-decoration: none;
    transition: all 0.3s ease;
    z-index: 1000;
}
.return-link:hover {
    color: #fff;
    background: var(--color-accent-blue);
}

@keyframes fadeIn {
    to { opacity: 1; }
}
</style>

/* *****************************************************************
 * 文件二~四：在 src/pages/ 目录下创建3个新的占位文件。
 * 这将彻底解决404错误。
 * *****************************************************************/

// 文件名: src/pages/core-theory.md
---
layout: ../layouts/ArticleLayout.astro
title: "天道 · 建设中"
---

此区域正在构建中，敬请期待。

行者正在此地，探寻宇宙的根本法则。

---

// 文件名: src/pages/case-studies.md
---
layout: ../layouts/ArticleLayout.astro
title: "地道 · 建设中"
---

此区域正在构建中，敬请期待。

行者正在此地，勘探现实世界的运行规律。

---

// 文件名: src/pages/handbook.md
---
layout: ../layouts/ArticleLayout.astro
title: "人道 · 独行者手册"
---

此区域正在构建中，敬请期待。

行者正在此地，审视内心的暗域与光明。
